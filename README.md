# AutoOrganizer

An automatic organizer for your downloads folder. Sorts files into categories by moving them to corresponding folders.

## 🚀 Features
- Sorts files by type: images, code, documents, disk images, archives
- Creates necessary folders automatically
- Has a test mode to check operation without moving files
- Supports custom paths via command-line arguments

## 📦 Installation
1. Clone the repository:
```bash
git clone <your-repository-url>
cd AutoOrganizer
